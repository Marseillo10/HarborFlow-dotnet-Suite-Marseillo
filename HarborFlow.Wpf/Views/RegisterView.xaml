<Window x:Class="HarborFlow.Wpf.Views.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HarborFlow.Wpf.Views"
        xmlns:converters="clr-namespace:HarborFlow.Wpf.Converters"
        mc:Ignorable="d"
        Title="HarborFlow - Register"
        Height="600"
        Width="400"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundColor}">
    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    <Grid>
        <Border Background="{DynamicResource PrimaryBackgroundColor}" 
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                CornerRadius="8"
                Padding="40">
            <Border.Effect>
                <DropShadowEffect Color="#FFCCCCCC" Direction="320" ShadowDepth="3" BlurRadius="5" Opacity="0.3"/>
            </Border.Effect>
            <StackPanel Width="280">
                <TextBlock Text="Create Account" 
                           FontSize="28"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>

                <TextBlock Text="Username" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

                <TextBlock Text="Password" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" Margin="0,0,0,15" PasswordChanged="PasswordBox_PasswordChanged"/>

                <TextBlock Text="Confirm Password" Margin="0,0,0,5"/>
                <PasswordBox x:Name="ConfirmPasswordBox" Margin="0,0,0,15" PasswordChanged="ConfirmPasswordBox_PasswordChanged"/>

                <TextBlock Text="Email" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

                <TextBlock Text="Full Name" Margin="0,0,0,5"/>
                <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

                <TextBlock Text="{Binding ErrorMessage}" 
                           Foreground="Red" 
                           Visibility="{Binding IsErrorVisible, Converter={StaticResource BoolToVis}}"
                           Margin="0,0,0,10"/>

                <Button Content="Register" 
                        Command="{Binding RegisterCommand}" 
                        Margin="0,10,0,0" 
                        Height="40" 
                        FontWeight="Bold"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                    <TextBlock Text="Already have an account? " VerticalAlignment="Center"/>
                    <Button Content="Login"
                            Click="LoginButton_Click"
                            Style="{StaticResource HyperlinkButtonStyle}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>