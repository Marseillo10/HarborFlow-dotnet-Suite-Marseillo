<UserControl x:Class="HarborFlow.Wpf.Views.UserProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HarborFlow.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="User Profile" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Username"/>
            <TextBox Text="{Binding Username}" Margin="0,5,0,10"/>

            <TextBlock Text="Email"/>
            <TextBox Text="{Binding Email}" Margin="0,5,0,10"/>

            <Button Content="Update Profile" Command="{Binding UpdateProfileCommand}" Margin="0,10,0,0"/>

            <Separator Margin="0,20,0,20"/>

            <TextBlock Text="Change Password" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

            <TextBlock Text="Current Password"/>
            <PasswordBox x:Name="CurrentPasswordBox" Margin="0,5,0,10"/>

            <TextBlock Text="New Password"/>
            <PasswordBox x:Name="NewPasswordBox" Margin="0,5,0,10"/>

            <TextBlock Text="Confirm New Password"/>
            <PasswordBox x:Name="ConfirmPasswordBox" Margin="0,5,0,10"/>

            <Button Content="Change Password" Command="{Binding ChangePasswordCommand}" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>