<Window x:Class="HarborFlow.Wpf.Views.VesselEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HarborFlow.Wpf.Views"
        xmlns:viewmodels="clr-namespace:HarborFlow.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="Vessel Editor" Height="auto" Width="400"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="IMO:"/>
            <TextBox Text="{Binding Vessel.IMO, ValidatesOnNotifyDataErrors=True, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent}" Foreground="Red"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Name:"/>
            <TextBox Text="{Binding Vessel.Name, ValidatesOnNotifyDataErrors=True, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent}" Foreground="Red"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Flag State:"/>
            <TextBox Text="{Binding Vessel.FlagState, ValidatesOnNotifyDataErrors=True, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent}" Foreground="Red"/>
        </StackPanel>

        <StackPanel Grid.Row="3">
            <TextBlock Text="Type:"/>
            <ComboBox ItemsSource="{Binding VesselTypes}" SelectedItem="{Binding Vessel.VesselType, ValidatesOnNotifyDataErrors=True, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent}" Foreground="Red"/>
        </StackPanel>

        <StackPanel Grid.Row="4">
            <TextBlock Text="MMSI:"/>
            <TextBox Text="{Binding Vessel.Mmsi, ValidatesOnNotifyDataErrors=True, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent}" Foreground="Red"/>
        </StackPanel>
        
        <StackPanel Grid.Row="5">
            <TextBlock Text="Length Overall:"/>
            <TextBox Text="{Binding Vessel.LengthOverall, ValidatesOnNotifyDataErrors=True, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent}" Foreground="Red"/>
        </StackPanel>

        <StackPanel Grid.Row="6">
            <TextBlock Text="Beam:"/>
            <TextBox Text="{Binding Vessel.Beam, ValidatesOnNotifyDataErrors=True, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent}" Foreground="Red"/>
        </StackPanel>

        <StackPanel Grid.Row="7">
            <TextBlock Text="Gross Tonnage:"/>
            <TextBox Text="{Binding Vessel.GrossTonnage, ValidatesOnNotifyDataErrors=True, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent}" Foreground="Red"/>
        </StackPanel>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}" IsDefault="True" MinWidth="80" Margin="0,0,10,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" IsCancel="True" MinWidth="80"/>
        </StackPanel>
    </Grid>
</Window>