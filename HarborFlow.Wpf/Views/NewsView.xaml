<UserControl x:Class="HarborFlow.Wpf.Views.NewsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HarborFlow.Wpf.Views"
             xmlns:viewmodels="clr-namespace:HarborFlow.Wpf.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:NewsViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="Maritime News" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
                        <Button Command="{Binding LoadNewsCommand}" Margin="20,0,0,0" Padding="10,5" Content="Refresh News"/>
            <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="20,0,5,0" FontWeight="SemiBold"/>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="250" VerticalAlignment="Center"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <!-- Loading Indicator -->
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="Loading News..." FontSize="18"/>
                <ProgressBar IsIndeterminate="True" Width="200" Height="15" Margin="0,10,0,0"/>
            </StackPanel>

            <!-- News Articles -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}, ConverterParameter=invert}">
                <ItemsControl ItemsSource="{Binding Articles}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Margin="10" Padding="15" Width="350">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="16" TextWrapping="Wrap" Margin="0,0,0,5"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <TextBlock Text="{Binding Source}" Foreground="Gray" FontSize="12"/>
                                        <TextBlock Text=" - " Foreground="Gray" FontSize="12"/>
                                        <TextBlock Text="{Binding PublishDate, StringFormat='g'}" Foreground="Gray" FontSize="12"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" MaxHeight="60" TextTrimming="CharacterEllipsis" Margin="0,0,0,10"/>
                                    <TextBlock>
                                        <Hyperlink NavigateUri="{Binding Link}" RequestNavigate="Hyperlink_RequestNavigate">
                                            Read More
                                        </Hyperlink>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
