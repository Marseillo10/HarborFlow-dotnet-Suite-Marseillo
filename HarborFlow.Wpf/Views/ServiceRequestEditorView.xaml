<Window x:Class="HarborFlow.Wpf.Views.ServiceRequestEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HarborFlow.Wpf.Views"
        mc:Ignorable="d"
        Title="Service Request Editor" Height="auto" Width="400"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Vessel IMO:"/>
            <TextBox Text="{Binding ServiceRequest.VesselImo, ValidatesOnNotifyDataErrors=True, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent}" Foreground="Red"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Service Type:"/>
            <ComboBox ItemsSource="{Binding ServiceTypes}" SelectedItem="{Binding ServiceRequest.ServiceType, ValidatesOnNotifyDataErrors=True, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent}" Foreground="Red"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Description:"/>
            <TextBox Text="{Binding ServiceRequest.Description, ValidatesOnNotifyDataErrors=True, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" Height="80"/>
            <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent}" Foreground="Red"/>
        </StackPanel>

        <StackPanel Grid.Row="3">
            <TextBlock Text="Request Date:"/>
            <DatePicker SelectedDate="{Binding ServiceRequest.RequestDate, ValidatesOnNotifyDataErrors=True, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding (Validation.Errors)[0].ErrorContent}" Foreground="Red"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}" IsDefault="True" MinWidth="80" Margin="0,0,10,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" IsCancel="True" MinWidth="80"/>
        </StackPanel>
    </Grid>
</Window>