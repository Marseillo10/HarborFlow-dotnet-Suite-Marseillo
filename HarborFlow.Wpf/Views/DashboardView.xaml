<UserControl x:Class="HarborFlow.Wpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HarborFlow.Wpf.Views"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/Images/MainBackground.jpg" Stretch="UniformToFill" Opacity="0.1"/>
        </Grid.Background>
        
        <StackPanel>
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Dashboard" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource PrimaryButtonStyle}" VerticalAlignment="Center"/>
            </Grid>
            
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Vessel Count Card -->
                <Border Grid.Column="0" Style="{StaticResource DashboardCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Path Data="{StaticResource ShipIcon}" Fill="{DynamicResource PrimaryTextColor}" Width="48" Height="48" Stretch="Uniform" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Total Vessels" Style="{StaticResource CardTitleStyle}"/>
                            <TextBlock Text="{Binding VesselCount}" Style="{StaticResource CardValueStyle}" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Active Service Requests Card -->
                <Border Grid.Column="1" Style="{StaticResource DashboardCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Path Data="{StaticResource ClipboardIcon}" Fill="{DynamicResource PrimaryTextColor}" Width="48" Height="48" Stretch="Uniform" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Active Services" Style="{StaticResource CardTitleStyle}"/>
                            <TextBlock Text="{Binding ActiveServiceRequestCount}" Style="{StaticResource CardValueStyle}" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Online Status Card -->
                <Border Grid.Column="2" Style="{StaticResource DashboardCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Path Data="{StaticResource StatusCircleIcon}" Fill="{Binding OnlineStatusColor}" Width="48" Height="48" Stretch="Uniform" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="System Status" Style="{StaticResource CardTitleStyle}"/>
                            <TextBlock Text="{Binding OnlineStatus}" Style="{StaticResource CardValueStyle}" FontSize="28" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <!-- Charts Section -->
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Service Requests Chart -->
                <Border Grid.Column="0" Style="{StaticResource DashboardCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Service Requests by Status" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <lvc:PieChart Series="{Binding ServiceRequestStatusSeries}" LegendLocation="Bottom" Height="250">
                            <lvc:PieChart.ChartLegend>
                                <lvc:DefaultLegend BulletSize="12"/>
                            </lvc:PieChart.ChartLegend>
                        </lvc:PieChart>
                    </StackPanel>
                </Border>

                <!-- Vessel Types Chart -->
                <Border Grid.Column="1" Style="{StaticResource DashboardCardStyle}">
                     <StackPanel>
                        <TextBlock Text="Vessels by Type" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <lvc:CartesianChart Series="{Binding VesselTypeSeries}" LegendLocation="Bottom" Height="250">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Title="Vessel Type" Labels="{Binding VesselTypeLabels}"/>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Title="Count" LabelFormatter="{Binding Formatter}"/>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                </Border>

                <!-- Vessels by Country Chart -->
                <Border Grid.Column="2" Style="{StaticResource DashboardCardStyle}">
                     <StackPanel>
                        <TextBlock Text="Vessels by Country" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <lvc:CartesianChart Series="{Binding VesselCountrySeries}" LegendLocation="Bottom" Height="250">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Title="Country" Labels="{Binding VesselCountryLabels}"/>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Title="Count" LabelFormatter="{Binding Formatter}"/>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                </Border>
            </Grid>

            <!-- Service Requests per Vessel Chart -->
            <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource DashboardCardStyle}" Margin="10,0,10,10">
                <StackPanel>
                    <TextBlock Text="Service Requests per Vessel" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <lvc:CartesianChart Series="{Binding ServiceRequestVesselSeries}" LegendLocation="Bottom" Height="250">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Vessel" Labels="{Binding ServiceRequestVesselLabels}"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Count" LabelFormatter="{Binding Formatter}"/>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </StackPanel>
            </Border>

            <!-- Service Requests per Month Chart -->
            <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource DashboardCardStyle}" Margin="10,0,10,10">
                <StackPanel>
                    <TextBlock Text="Service Requests per Month" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <lvc:CartesianChart Series="{Binding ServiceRequestMonthSeries}" LegendLocation="Bottom" Height="250">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Month" Labels="{Binding ServiceRequestMonthLabels}"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Count" LabelFormatter="{Binding Formatter}"/>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </StackPanel>
            </Border>

            <!-- Latest News Section -->
            <Border Grid.Row="2" Style="{StaticResource DashboardCardStyle}" Margin="10,0,10,10">
                <StackPanel>
                    <TextBlock Text="Latest Maritime News" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <ItemsControl ItemsSource="{Binding LatestNews}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,5,0,5">
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Source}" Foreground="Gray"/>
                                        <TextBlock Text=" - " Foreground="Gray"/>
                                        <TextBlock Text="{Binding PublishDate, StringFormat='g'}" Foreground="Gray"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>